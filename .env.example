# ============================================
# Sportify Environment Variables Configuration
# ============================================
# Kopiere diese Datei zu .env und fülle die Werte aus
# Copy this file to .env and fill in the values
# ============================================

# Database Connection
# PostgreSQL connection string (e.g., Neon, Supabase, or local PostgreSQL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=

# Database SSL Configuration (optional)
# Set to true for hosted databases that require SSL (e.g., Neon, Supabase)
DATABASE_SSL_ENABLED=false
# Set to false if using self-signed certificates
DATABASE_SSL_REJECT_UNAUTHORIZED=true

# Backend Server Configuration
# Port for the Express backend server
PORT=3001

# Frontend Configuration
# API URL for frontend to connect to backend
VITE_API_URL=http://localhost:3001/api
# Frontend URL (used for email links and CORS)
FRONTEND_URL=http://localhost:8080
# Frontend URL for client-side config (keep in sync with FRONTEND_URL)
VITE_FRONTEND_URL=http://localhost:8080
# Comma-separated list of allowed CORS origins (overrides default localhost list)
# Example: CORS_ALLOW_ORIGINS=https://sportify.app,https://www.sportify.app
CORS_ALLOW_ORIGINS=

# JWT Secret
# IMPORTANT: Use a strong, random secret key in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Email Configuration (SMTP)
# Required for email verification, password reset, and invitations
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@sportify.com

# ===========================================
# Public Contact Information (Impressum/Privacy)
# ===========================================
# These values are public and shown in legal pages (Imprint/Privacy/Terms)
VITE_CONTACT_RESPONSIBLE_PERSON="Leon Stadler"
VITE_CONTACT_EMAIL="sportify@leon-stadler.com"
VITE_CONTACT_PHONE="+4917635491384"
VITE_CONTACT_ADDRESS_STREET="Uferstraße 42"
VITE_CONTACT_ADDRESS_POSTAL_CODE="88145"
VITE_CONTACT_ADDRESS_CITY="Nonnenhorn"
VITE_CONTACT_ADDRESS_COUNTRY="Deutschland"

# ===========================================
# Web Push Notifications (VAPID Keys)
# ===========================================
# VAPID Keys werden für Push-Benachrichtigungen benötigt.
#
# So generierst du die Keys (einmalig):
# 1. Führe aus: npx web-push generate-vapid-keys
# 2. Kopiere die generierten Keys hier ein
#
# Alternativ online: https://vapidkeys.com/
#
# Der Public Key wird automatisch vom Backend ans Frontend gesendet.

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:deine@email.com

# ===========================================
# Event Scheduler (GitHub Actions / Cron)
# ===========================================
# Diese Variablen werden für den GitHub Actions Workflow benötigt,
# der die Event-Auswertungen (wöchentlich, monatlich, E-Mail-Versand) triggert.
#
# EVENTS_BASE_URL: Die URL deiner deployed API (z.B. https://vertic-id.com)
#                  Falls nicht gesetzt, wird FRONTEND_URL verwendet.
# EVENTS_CRON_SECRET: Ein Secret-Token für die Authentifizierung der Cron-Requests.
#                     Muss mit dem Secret in GitHub Actions übereinstimmen.
#                     Generiere mit: openssl rand -base64 32

EVENTS_BASE_URL=
EVENTS_CRON_SECRET=

# Node Environment
# Set to 'production' for production deployments
# NODE_ENV=development

# ===========================================
# Vercel: Migrationen laufen beim Build (npm run migrate), nicht beim Cold Start.
# DATABASE_URL muss für den Build in Vercel gesetzt sein.
# ===========================================
