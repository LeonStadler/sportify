# ===========================================
# GitHub Actions Secrets
# ===========================================
# Diese Secrets müssen in GitHub unter Settings > Secrets and variables > Actions angelegt werden.
# Sie werden vom events-scheduler.yml Workflow verwendet, um die Event-Auswertungen zu triggern.
#
# Anleitung:
# 1. Gehe zu deinem GitHub Repository
# 2. Klicke auf Settings > Secrets and variables > Actions
# 3. Klicke auf "New repository secret"
# 4. Füge die folgenden Secrets hinzu:
#
# ===========================================
# Erforderliche Secrets
# ===========================================

# EVENTS_BASE_URL
# Die URL deiner deployed API (z.B. https://www.vertic-id.com)
# Muss mit http:// oder https:// beginnen
# Beispiel: https://www.vertic-id.com
EVENTS_BASE_URL="https://www.vertic-id.com"

# EVENTS_CRON_SECRET
# Ein Secret-Token für die Authentifizierung der Cron-Requests.
# Muss mit dem EVENTS_CRON_SECRET in deiner Vercel-Umgebung übereinstimmen.
# Generiere ein neues Secret mit: openssl rand -base64 32
EVENTS_CRON_SECRET=

# ===========================================
# Hinweise
# ===========================================
# - Beide Secrets sind erforderlich, damit der Workflow funktioniert
# - Der Workflow wird automatisch übersprungen, wenn Secrets fehlen
# - EVENTS_CRON_SECRET muss in GitHub Actions UND in Vercel identisch sein
# - EVENTS_BASE_URL sollte auf deine Production-URL zeigen (ohne trailing slash)
